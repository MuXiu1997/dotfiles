save-prefix=

# npm follow XDG specification
prefix=${XDG_DATA_HOME}/npm
cache=${XDG_CACHE_HOME}/npm
tmp=${XDG_RUNTIME_DIR}/npm

# npm init defaults
init-author-name=MuXiu1997
init-author-email=muxiu1997@muxiu1997.com
init-author-url=https://github.com/MuXiu1997
init-license=MIT
init-version=0.1.0
init-type=module
init-private=true

# pnpm follow XDG specification
prefer-offline=true
store-dir=${XDG_DATA_HOME}/pnpm/store
global-dir=${XDG_DATA_HOME}/pnpm/global
global-bin-dir=${XDG_DATA_HOME}/pnpm/bin
state-dir=${XDG_STATE_HOME}/pnpm
cache-dir=${XDG_CACHE_HOME}/pnpm

{{ if has "personal" .tags -}}
//registry.npmjs.org/:_authToken={{ template "ageDecrypted" "npm-token" }}
{{- end }}
